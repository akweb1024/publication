global:
  resolve_timeout: 5m

route:
  receiver: default
  group_wait: 20s
  group_interval: 2m
  repeat_interval: 30m

receivers:
  - name: default
    webhook_configs:
      - url: http://127.0.0.1:5001/alerts
        send_resolved: true
